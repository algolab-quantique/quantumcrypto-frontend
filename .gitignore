# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
.idea
# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — each developer's own machine config, never committed.
# `.env.example` IS tracked: it is the template new contributors copy from.
.env
.env.local
.env*.local


# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# tasks_todo.md # we need to track our tasks and what we have done

# DEPLOYMENT_GUIDE.md moved 2026-08-27 to the private deploy repo
# (cryptoweb-2.0-frontend), where it is tracked. It described our internal
# dual-repo Amplify setup, which does not apply to public users of this repo.
